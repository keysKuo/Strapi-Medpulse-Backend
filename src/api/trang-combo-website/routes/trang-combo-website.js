'use strict';

/**
 * trang-combo-website router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::trang-combo-website.trang-combo-website');
