'use strict';

/**
 * trang-combo-website service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::trang-combo-website.trang-combo-website');
