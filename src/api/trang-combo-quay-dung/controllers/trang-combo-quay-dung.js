'use strict';

/**
 * trang-combo-quay-dung controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::trang-combo-quay-dung.trang-combo-quay-dung');
