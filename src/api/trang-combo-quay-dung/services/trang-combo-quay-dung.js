'use strict';

/**
 * trang-combo-quay-dung service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::trang-combo-quay-dung.trang-combo-quay-dung');
